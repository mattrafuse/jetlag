# syntax=docker/dockerfile:1

# Custom nginx image with the app's reverse-proxy config baked in, so the
# config does not need to be bind-mounted (and copied to the host) at runtime.
FROM nginx:alpine

COPY nginx.conf /etc/nginx/nginx.conf
